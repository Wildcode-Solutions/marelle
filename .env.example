# Vide pour le Web : le front et l'API Worker partagent le même domaine.
# Pour Capacitor, renseigner ici l'URL HTTPS publique du Worker.
VITE_API_URL=

# Client OAuth Google public, uniquement si l'authentification Google est activée.
VITE_GOOGLE_CLIENT_ID=
